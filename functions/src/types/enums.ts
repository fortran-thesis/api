export enum Role {
  USER = "user",
  ADMIN = "admin",
  CURATOR = "curator",
}

export enum FeedbackStatus {
  PENDING = 0,
  REVIEWED = 1,
}

export enum PhotoSource {
  CAMERA = 0,
  UPLOAD = 1,
}
